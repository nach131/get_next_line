si en buf hay '\n' y mas character
	poner linea en res
	poner resto en tp
si hay character en tp
	comprobar que hay linea en tp
	poner linea en rest
	quitar el resto de tp



cur->path = malloc(strlen(*path)+1);
printf("Malloc path success");
strcpy(cur->path, *path);